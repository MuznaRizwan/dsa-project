#include "basescreen.h"
void BaseScreen::handleEvents(SDL_Event event) {
} //-keymouse keypress
void BaseScreen::cleanUp() {
} //-clean free surfaces
void BaseScreen::load(SDL_Renderer* renderer) {
} //-load the textures, font
void BaseScreen::render() {
} //-screen draw
